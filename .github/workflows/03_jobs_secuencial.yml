name: Jobs Secuenciales
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Construit proyecto
        run: echo "Construyendo proyecyo..."

  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Ejecutar tests
        run: echo "Ejecutando tests..."